json.extract! package_occupiedby, :id, :start_date, :end_date, :balance_session, :trainer_package, :member_id, :created_at, :updated_at
json.url package_occupiedby_url(package_occupiedby, format: :json)
