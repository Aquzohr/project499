

<%= bootstrap_form_for(package, :html=> {:id => 'new_t_package'}) do |form| %>

    <%= form.text_field :name %>
    <%= form.text_field :price %>
    
    <%= form.fields_for :trainer_package, TrainerPackage.new  do |f| %>
      <%= f.text_field :session %>
    <% end %>
    
    <%= form.submit %>

<% end %>

