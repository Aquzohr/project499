<table  data-toggle="table"
        data-pagination-pre-text="Previous"
        data-pagination-next-text="Next" >
  <thead>
    <tr>
      <th data-width="300px">Name</th>
      <th data-width="300px">Price</th>
      <th>Freeze Time</th>
      <th data-width="100px"></th>
    </tr>
  </thead>
  <tbody>
    <% packages.each do |p| %>
      <tr>
        <td><%= p.package.name %></td>
        <td><%= number_to_currency(p.package.price, unit: "฿") %></td>
        <td><%= p.freeze_time %></td>
        <td><%= link_to 'Edit', edit_nontrainer_package_path(p), class: 'btn btn-warning btn-block', :"data-turbolinks" => false %></td>
      </tr>
    <% end %>
  </tbody>
  
</table>