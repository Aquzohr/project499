<%= bootstrap_form_for(nontrainer_package) do |f| %>
  <% if nontrainer_package.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(nontrainer_package.errors.count, "error") %> prohibited this nontrainer_package from being saved:</h2>

      <ul>
      <% nontrainer_package.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>


  <%= f.text_field :name %>
  <%= f.number_field :price %>
  <%= f.number_field :freeze_time %>
  <%= f.submit %>

<% end %>
